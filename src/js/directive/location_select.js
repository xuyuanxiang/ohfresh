define([''], function () {
    angular.module('ohFresh.plugins')
        .directive('locationSelect',
        function () {
            return {
                restrict: 'AE',
                controller: function ($scope) {

                }
            }
        }
    );
});